apiVersion: v1
kind: Service
metadata:
  name: trend-app-service
  labels:
    app: trend-app
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-type: "nlb"
spec:
  selector:
    app: trend-app
  type: LoadBalancer
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000

